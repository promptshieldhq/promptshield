

# Auth
BETTER_AUTH_SECRET=change-me-at-least-32-characters-long

# Database
POSTGRES_PASSWORD=password

# Audit log ingest (shared secret between log-fwd sidecar and server)
AUDIT_INGEST_SECRET=dev-ingest-secret

# Optional forwarding controls:
# - docker: tails docker logs from GATEWAY_CONTAINER_NAME
# - file:   tails GATEWAY_LOG_FILE (for standalone binary logs)
# - stdin:  reads from stdin
AUDIT_SERVER_URL=http://ps-server:3000
GATEWAY_LOG_SOURCE=docker
GATEWAY_CONTAINER_NAME=promptshield-gateway
GATEWAY_LOG_FILE=/gateway-logs/promptshield.log

# Engine auth forwarding (BFF -> engine)
ENGINE_API_KEY=dev-engine-key

# Dashboard config admin access control
# Comma-separated email addresses allowed to mutate gateway/policy settings.
CONFIG_ADMIN_EMAILS=you@example.com

POLICY_ALLOWED_DIRS=true

# Optional external gateway policy management (dashboard -> gateway API)
# POLICY_SOURCE=local_file | gateway_api
POLICY_SOURCE=local_file
GATEWAY_POLICY_ENDPOINT=/admin/policy
GATEWAY_ADMIN_TOKEN=dev-gateway-admin-token

# Optional external gateway config management (dashboard -> gateway API)
# GATEWAY_CONFIG_SOURCE=local_env | gateway_api
GATEWAY_CONFIG_SOURCE=local_env
GATEWAY_CONFIG_ENDPOINT=/admin/config

# Gateway; LLM provider (pick one)
PROMPTSHIELD_PROVIDER=gemini   # gemini | openai | anthropic | selfhosted
#PROMPTSHIELD_PROVIDERS=gemini,openai,anthropic,selfhosted
GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# The vars below are injected by docker-compose.dev.yml directly
# DATABASE_URL      → built from POSTGRES_PASSWORD
# BETTER_AUTH_URL   → http://localhost:3000
# CORS_ORIGIN       → http://localhost:8000
# ENGINE_URL        → http://engine:4321
# GATEWAY_URL       → http://promptshield-gateway:8080
# VITE_SERVER_URL   → http://localhost:3000
