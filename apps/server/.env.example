BETTER_AUTH_SECRET=your-secret-here-min-32-chars
BETTER_AUTH_URL=http://localhost:3000
CORS_ORIGIN=http://localhost:8000

DATABASE_URL=postgresql://postgres:password@localhost:5432/promptshield

# External services (engine + proxy run as separate stacks)
ENGINE_URL=http://localhost:4321
ENGINE_API_KEY=dev-engine-key
PROXY_URL=http://localhost:8080
PROXY_CONFIG_SOURCE=local_env
PROXY_CONFIG_ENDPOINT=/admin/config
CONFIG_ADMIN_EMAILS=you@example.com

# Policy source:
# - local_file: read/write POLICY_FILE_PATH in this repo stack
# - proxy_api:  read/write external proxy policy endpoint
POLICY_SOURCE=local_file
PROXY_POLICY_ENDPOINT=/admin/policy
PROXY_ADMIN_TOKEN=dev-proxy-admin-token

# Audit forwarding controls (used by scripts/log-fwd.sh)
# Plain setup defaults: read from a local file and send to local API server.
# For Docker log tailing, set PROXY_LOG_SOURCE=docker and PROXY_CONTAINER_NAME.
AUDIT_SERVER_URL=http://localhost:3000
PROXY_LOG_SOURCE=file
PROXY_CONTAINER_NAME=promptshield-gateway
PROXY_LOG_FILE=../../proxy-logs/promptshield.log

POLICY_FILE_PATH=../../config/policy.yaml
POLICY_ALLOWED_DIRS=
PROXY_ENV_PATH=../../config/proxy.env
AUDIT_INGEST_SECRET=dev-ingest-secret
